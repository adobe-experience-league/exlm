sitemaps:
  example:
    default: en
    languages:
      en:
        source: /en/query-index.json
        destination: /sitemap-en.xml
        hreflang: en
      fr:
        source: /fr/query-index.json
        destination: /sitemap-fr.xml
        hreflang: fr
        alternate: /fr/{path}
      de:
        source: /de/query-index.json
        destination: /sitemap-de.xml
        hreflang: de
        alternate: /de/{path}
      es:
        source: /es/query-index.json
        destination: /sitemap-es.xml
        hreflang: es
        alternate: /es/{path}
      it:
        source: /it/query-index.json
        destination: /sitemap-it.xml
        hreflang: it
        alternate: /it/{path}
      nl:
        source: /nl/query-index.json
        destination: /sitemap-nl.xml
        hreflang: nl
        alternate: /nl/{path}
      pt-br:
        source: /pt-br/query-index.json
        destination: /sitemap-pt-br.xml
        hreflang: pt-br
        alternate: /pt-br/{path}
      ko:
        source: /ko/query-index.json
        destination: /sitemap-ko.xml
        hreflang: ko
        alternate: /ko/{path}
      ja:
        source: /ja/query-index.json
        destination: /sitemap-ja.xml
        hreflang: ja
        alternate: /ja/{path}
      sv:
        source: /sv/query-index.json
        destination: /sitemap-sv.xml
        hreflang: sv
        alternate: /sv/{path}
      zh-hans:
        source: /zh-hans/query-index.json
        destination: /sitemap-zh-hans.xml
        hreflang: zh-hans
        alternate: /zh-hans/{path}
      zh-hant:
        source: /zh-hant/query-index.json
        destination: /sitemap-zh-hant.xml
        hreflang: zh-hant
        alternate: /zh-hant/{path}
