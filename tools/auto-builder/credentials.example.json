{
  "//": "Copy this file to credentials.json (gitignored) and fill in one entry per JIRA assignee whose tickets this poller should build. Key = the assignee's JIRA account email (case-insensitive). Any field left blank falls back to the repo-root .env value (JIRA_PAT / GITHUB_TOKEN) or the poller process's own CLAUDE_CODE_OAUTH_TOKEN. Tickets whose assignee has no entry here also fall back to those .env/process defaults.",
  "jane.doe@adobe.com": {
    "jiraPat": "<jane's JIRA personal access token>",
    "githubToken": "<jane's GitHub fine-grained PAT, scoped to this repo only>",
    "claudeToken": "<jane's CLAUDE_CODE_OAUTH_TOKEN, from `claude setup-token`>"
  },
  "john.smith@adobe.com": {
    "jiraPat": "<john's JIRA personal access token>",
    "githubToken": "<john's GitHub fine-grained PAT, scoped to this repo only>",
    "claudeToken": "<john's CLAUDE_CODE_OAUTH_TOKEN, from `claude setup-token`>"
  }
}
